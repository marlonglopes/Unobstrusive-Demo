$("#<%= dom_id(@task) %>").replaceWith("<%= escape_javascript(render(:partial => @task)) %>");
<% @task = Task.new # reset for new form %>
$(".edit_task").replaceWith("<%= escape_javascript(render(:partial => "form"))%>")
$(".new_task")[0].reset();
